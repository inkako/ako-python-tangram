[project]
name = "ako-python-tangram"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "wuchenghui", email = "chenghui.wu@foxmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[all]>=0.141.1",
]

[project.scripts]
ako-python-tangram = "ako_python_tangram:main"

[build-system]
requires = ["uv_build>=0.11.30,<0.12.0"]
build-backend = "uv_build"
